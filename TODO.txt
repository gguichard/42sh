TODO:

	Multi tokens.
		- Si quote non fermé faire multi tokens ou considerer comme dans quote ?!?
	Test a regler:

		$> cat
		kill term

		$! a faire

		ls -R / | sleep 10 | ./segfault | cat > po | ls -z | ./bus_error
		sleep 2 | ls -R / | wc -l &
		ls | qw | sleep 2 | ls -z &
		leaks exit signal
